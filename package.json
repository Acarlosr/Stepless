{
  "name": "stepless",
  "version": "1.0.0",
  "type": "module",
  "description": "Stepless — Decentralized accessibility mapping on Arc Testnet",
  "scripts": {
    "test": "node --test",
    "check": "node --check api/_stepless.js && node --check api/rpc.js && node --check api/relay.js && node --check api/verify.js && node --check api/_placecheck.js && node --check api/_risk.js && node --check frontend/dashboard.js && node scripts/sync-distributor-bytecode.mjs --check",
    "sync:bytecode": "node scripts/sync-distributor-bytecode.mjs",
    "preview": "node scripts/preview.mjs"
  },
  "dependencies": {
    "viem": "^2.21.0",
    "exifr": "^7.1.3"
  },
  "engines": {
    "node": ">=18"
  }
}
